version: '3'
networks:
  caddy:
services:
  mimi-api:
    container_name: mimi-api
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    build:
      context: ./
    networks:
      - caddy
    ports:
      - 4000:4000

  caddy:
    image: caddy:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./site:/srv
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - caddy

volumes:
  caddy_data:
  caddy_config:
